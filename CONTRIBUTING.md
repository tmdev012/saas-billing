# Contributing
1. Fork → branch (`feat/xxx`) → PR against `main`
2. Commit convention: `type(scope): description` (feat/fix/docs/ci/test/security)
3. Run `make check` before pushing
4. All PRs require CI to pass
